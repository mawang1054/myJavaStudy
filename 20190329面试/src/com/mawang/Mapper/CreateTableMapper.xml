<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mawang.Mapper.CreateTableMapper">
	<update id="createTable" >
		CREATE TABLE user ( 
			`id` INT NOT NULL AUTO_INCREMENT, 
			`username` VARCHAR(30), 
			`password` VARCHAR(30), 
			`gender` VARCHAR(2), 
			`create_at` DATE, 
			PRIMARY KEY (`id`) 
		); 
	</update>
</mapper>